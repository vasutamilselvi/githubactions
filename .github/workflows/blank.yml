name: my-workflow

on: workflow_dispatch

jobs:
  build:
    steps:
      name: my 1st tep
      run echo "my 1 st step"
      name: my 2nd step
      run echo "my 2 nd step"
  deploy:
    steps:
      name: my 3rd step
      run echo "my 3rd step"
      name: my 4th step
      run echo "my 4th step"
